apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  labels:
    mcp-server: "true"
  name: mcp-server3-b-route
  namespace: mcp-test
spec:
  hostnames:
  - server3.team-b.mcp.local
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: team-b-gateway
    namespace: gateway-system
  rules:
  - backendRefs:
    - group: ""
      kind: Service
      name: mcp-test-server3
      port: 9090
      weight: 1
    matches:
    - path:
        type: PathPrefix
        value: /
